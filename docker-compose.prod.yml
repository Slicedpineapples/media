services:
  media:
    image: ghcr.io/slicedpineapples/media-upload-service:latest
    ports:
      - "8005:8005"
    volumes:
      - uploads:/app/uploads
    env_file:
      - .env
    environment:
      PORT: 8005
    restart: unless-stopped

volumes:
  uploads:
